name: Deploy

on:
  push:
    branches:
      - main # includes the case when pull request is merged to main 

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Log deployment message
      run: echo "Deployed!"
